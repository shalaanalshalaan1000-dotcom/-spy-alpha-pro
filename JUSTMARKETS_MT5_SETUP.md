# ربط SPY Alpha Pro بحساب JustMarkets التجريبي

الموقع لا يستلم كلمة مرور حساب التداول. التنفيذ يتم داخل منصة MetaTrader 5 بواسطة المستشار الآلي `GoldAlphaExecutor.mq5`، بينما يرسل الموقع إشارة قصيرة العمر عبر HTTPS.

> الإعداد الافتراضي تجريبي فقط. لا تغيّر `RequireDemoAccount=true` قبل اختبار مستقل طويل وفهم مخاطر تداول الذهب بعقود الفروقات.

## ضوابط الأمان المفعّلة

- مخاطرة قصوى لكل صفقة: `0.25%` من رصيد الحساب.
- حد خسارة يومي: `1%` من قيمة الحساب عند بداية يوم خادم الوسيط.
- صفقة واحدة فقط على رمز الذهب في الوقت نفسه.
- رفض الإشارة بعد انتهاء صلاحيتها، ورفض تكرارها.
- رفض الدخول خارج نطاق السعر أو عندما يتجاوز السبريد `0.80` دولار.
- رفض الصفقة إذا كان أقل حجم يسمح به الوسيط يتجاوز سقف المخاطرة.
- نقل وقف الخسارة: إلى التعادل عند الهدف الأول، ثم إلى الهدف السابق عند الهدفين الثاني والثالث.
- الهدف النهائي للأمر هو الهدف الرابع.

## التثبيت

1. نزّل MetaTrader 5 المكتبي من JustMarkets، وافتح حسابًا **تجريبيًا** وسجّل الدخول إليه.
2. في MT5 اختر `File > Open Data Folder` ثم افتح `MQL5/Experts`.
3. انسخ ملف `mt5/GoldAlphaExecutor.mq5` إلى هذا المجلد.
4. افتح MetaEditor، ثم افتح الملف واضغط `Compile`. يجب أن ينتهي بلا أخطاء.
5. في MT5 افتح `Tools > Options > Expert Advisors` وفعّل `Allow WebRequest for listed URL`، ثم أضف:

   `https://spy-alpha-pro-1.onrender.com`

6. افتح رسم رمز الذهب الدقيق لدى وسيطك (قد يكون `XAUUSD` أو اسمًا بلاحقة)، ثم اسحب المستشار إليه. اترك `TradeSymbol` فارغًا ليستخدم رمز الرسم.
7. اترك `RequireDemoAccount=true` و`EnableDemoTrading=true`، ثم فعّل زر `Algo Trading`.
8. راقب تبويبي `Experts` و`Journal`. ظهور خطأ WebRequest يعني غالبًا أن عنوان الموقع لم يُضف إلى القائمة المسموحة.

## مسارات الفحص

- عرض آمن لا يحجز إشارة: `/api/auto-trade/signal?observe=1`
- مسار التنفيذ الذي يقرأه MT5: `/api/auto-trade/signal`
- قراءة الذهب الخام: `/api/gold-live`

## ملاحظات مهمة

- يجب إبقاء MT5 يعمل على كمبيوتر أو VPS؛ تطبيق الهاتف لا يشغّل المستشارين الآليين.
- التداول على JustMarkets في XAUUSD يكون عادةً عبر عقد فروقات، وليس امتلاك ذهب فوري.
- لا تضع بيانات دخول MT5 أو JustMarkets في الموقع أو GitHub أو متغيرات Render.
- مسار الإشارة لا ينفّذ أي أمر لدى الوسيط ولا يحتوي بيانات الحساب؛ لذلك يبقى متاحًا لـ MT5 حتى لو فُعّلت حماية دخول لوحة الموقع.
- لا توجد صفقة مضمونة. توقف التغذية أو الانزلاق أو اتساع السبريد قد يغيّر نتيجة التنفيذ.
