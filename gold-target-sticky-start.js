import fs from 'node:fs';
import {syncBuiltinESMExports} from 'node:module';

const previousWriteFileSync=fs.writeFileSync.bind(fs);

function patchStickyGoldTargets(source){
  const oldFn="function renderGoldTargetProgress(plan){const targets=[plan.target1,plan.target2,plan.target3,plan.target4],etas=[plan.eta1,plan.eta2,null,null],price=Number(plan.currentPrice),hits=targets.map(t=>goldTargetReached(plan.state,price,t)),next=hits.findIndex(x=>!x);for(let i=0;i<4;i++){const node=$('#goldEta'+(i+1));if(!node)continue;if(!plan.locked||targets[i]==null||!Number.isFinite(Number(targets[i]))){node.textContent='—';node.className='';continue}if(hits[i]){node.textContent='✓ تحقق';node.className='targetHit';continue}if(i===next){node.textContent=(etas[i]?'المدة: '+etas[i]+' • ':'')+'الهدف الحالي';node.className='targetCurrent'}else{node.textContent=i===3?'الامتداد الأخير للإشارة':'امتداد بعد الهدف '+i;node.className=''}}}";
  const previousPatchedFn="function renderGoldTargetProgress(plan){const targets=[plan.target1,plan.target2,plan.target3,plan.target4],etas=[plan.eta1,plan.eta2,null,null],price=Number(plan.currentPrice),lock=readGoldTradeLock(),since=Number(plan.lockCreatedAt||lock?.createdAt||0),rows=(typeof goldSamples!=='undefined'&&Array.isArray(goldSamples)?goldSamples:[]).filter(x=>Number(x.t)>=since).map(x=>Number(x.price)).filter(Number.isFinite);rows.push(price);const stored=Array.isArray(lock?.tpHits)?lock.tpHits:[false,false,false,false],entryTouched=Boolean(plan.entryTouched||lock?.entryTouched),hits=targets.map((t,i)=>entryTouched&&(Boolean(stored[i])||rows.some(v=>goldTargetReached(plan.state,v,t))));if(lock&&plan.locked&&entryTouched&&hits.some((v,i)=>v!==Boolean(stored[i]))){lock.tpHits=hits;writeGoldTradeLock(lock)}const next=hits.findIndex(x=>!x);for(let i=0;i<4;i++){const node=$('#goldEta'+(i+1));if(!node)continue;if(!plan.locked||targets[i]==null||!Number.isFinite(Number(targets[i]))){node.textContent='—';node.className='';continue}if(!entryTouched){node.textContent='بانتظار دخول النطاق';node.className='';continue}if(hits[i]){node.textContent='✓ تحقق';node.className='targetHit';continue}if(i===next){node.textContent=(etas[i]?'المدة: '+etas[i]+' • ':'')+'الهدف الحالي';node.className='targetCurrent'}else{node.textContent=i===3?'الامتداد الأخير للإشارة':'امتداد بعد الهدف '+i;node.className=''}}}";
  const currentFn="function renderGoldTargetProgress(plan){const targets=[plan.target1,plan.target2,plan.target3,plan.target4],etas=[plan.eta1,plan.eta2,null,null],price=Number(plan.currentPrice),lock=readGoldTradeLock(),since=Number(plan.lockCreatedAt||lock?.createdAt||0),rows=(typeof goldSamples!=='undefined'&&Array.isArray(goldSamples)?goldSamples:[]).filter(x=>Number(x.t)>=since).map(x=>Number(x.price)).filter(Number.isFinite);if(Number.isFinite(price))rows.push(price);const entryLow=Number(plan.entryLow??lock?.entryLow??Number(plan.entry)-.5),entryHigh=Number(plan.entryHigh??lock?.entryHigh??Number(plan.entry)+.5),minSeen=rows.length?Math.min(...rows):price,maxSeen=rows.length?Math.max(...rows):price,stored=Array.isArray(lock?.tpHits)?lock.tpHits:[false,false,false,false];let entryTouched=Boolean(plan.entryTouched||lock?.entryTouched);if(!entryTouched&&Number.isFinite(entryLow)&&Number.isFinite(entryHigh)&&rows.length){entryTouched=rows.some(v=>v>=entryLow&&v<=entryHigh)||(minSeen<=entryHigh&&maxSeen>=entryLow)}if(lock&&plan.locked){lock.minSeen=Number.isFinite(Number(lock.minSeen))?Math.min(Number(lock.minSeen),minSeen):minSeen;lock.maxSeen=Number.isFinite(Number(lock.maxSeen))?Math.max(Number(lock.maxSeen),maxSeen):maxSeen;if(entryTouched&&!lock.entryTouched){lock.entryTouched=true;lock.entryTouchedAt=Date.now()}}const low=lock&&Number.isFinite(Number(lock.minSeen))?Number(lock.minSeen):minSeen,high=lock&&Number.isFinite(Number(lock.maxSeen))?Number(lock.maxSeen):maxSeen;const hits=targets.map((t,i)=>{if(!entryTouched||t==null||!Number.isFinite(Number(t)))return false;if(Boolean(stored[i]))return true;return plan.state==='UP'?high>=Number(t):low<=Number(t)});if(lock&&plan.locked){lock.tpHits=hits;writeGoldTradeLock(lock)}const next=hits.findIndex((x,i)=>!x&&targets[i]!=null&&Number.isFinite(Number(targets[i])));for(let i=0;i<4;i++){const node=$('#goldEta'+(i+1));if(!node)continue;if(!plan.locked||targets[i]==null||!Number.isFinite(Number(targets[i]))){node.textContent='—';node.className='';continue}if(!entryTouched){node.textContent='بانتظار دخول النطاق';node.className='';continue}if(hits[i]){node.textContent='✓ تحقق';node.className='targetHit';continue}if(i===next){node.textContent=(etas[i]?'المدة: '+etas[i]+' • ':'')+'الهدف الحالي';node.className='targetCurrent'}else{node.textContent=i===3?'الامتداد الأخير للإشارة':'امتداد بعد الهدف '+i;node.className=''}}}";
  const newFn="function renderGoldTargetProgress(plan){const targets=[plan.target1,plan.target2,plan.target3,plan.target4],etas=[plan.eta1,plan.eta2,null,null],price=Number(plan.currentPrice),lock=readGoldTradeLock(),since=Number(plan.lockCreatedAt||lock?.createdAt||0),rows=(typeof goldSamples!=='undefined'&&Array.isArray(goldSamples)?goldSamples:[]).filter(x=>Number(x.t)>=since).map(x=>Number(x.price)).filter(Number.isFinite);if(Number.isFinite(price))rows.push(price);const entryLow=Number(plan.entryLow??lock?.entryLow??Number(plan.entry)-.5),entryHigh=Number(plan.entryHigh??lock?.entryHigh??Number(plan.entry)+.5),minSeen=rows.length?Math.min(...rows):price,maxSeen=rows.length?Math.max(...rows):price,stored=Array.isArray(lock?.tpHits)?lock.tpHits:[false,false,false,false];let entryTouched=Boolean(plan.entryTouched||lock?.entryTouched);if(!entryTouched&&Number.isFinite(entryLow)&&Number.isFinite(entryHigh)&&rows.length){entryTouched=rows.some(v=>v>=entryLow&&v<=entryHigh)||(minSeen<=entryHigh&&maxSeen>=entryLow)}if(lock&&plan.locked){lock.minSeen=Number.isFinite(Number(lock.minSeen))?Math.min(Number(lock.minSeen),minSeen):minSeen;lock.maxSeen=Number.isFinite(Number(lock.maxSeen))?Math.max(Number(lock.maxSeen),maxSeen):maxSeen;if(entryTouched&&!lock.entryTouched){lock.entryTouched=true;lock.entryTouchedAt=Date.now()}}const low=lock&&Number.isFinite(Number(lock.minSeen))?Number(lock.minSeen):minSeen,high=lock&&Number.isFinite(Number(lock.maxSeen))?Number(lock.maxSeen):maxSeen,stop=Number(plan.invalidation??lock?.invalidation),stopHit=entryTouched&&Number.isFinite(stop)&&(plan.state==='UP'?low<=stop:high>=stop);if(stopHit){if(lock){lock.stopHit=true;lock.stoppedAt=Date.now();lock.tpHits=stored;try{if(typeof saveGoldHistorySnapshot==='function')saveGoldHistorySnapshot(lock,'SL')}catch{}try{if(typeof writeGoldCooldown==='function')writeGoldCooldown({until:Date.now()+90_000,reason:'STOP_HIT',stoppedAt:Date.now(),side:lock.state,stop})}catch{}try{writeGoldTradeLock(null)}catch{}}const status=$('#goldPlanStatus'),note=$('#goldPlanNote');if(status){status.textContent='وقف الخسارة تحقق';status.className='negative'}if(note)note.textContent='انتهى السيناريو — STOP LOSS HIT. لن تبقى هذه الصفقة فعالة.';for(let i=0;i<4;i++){const node=$('#goldEta'+(i+1));if(node&&!stored[i]){node.textContent='انتهى';node.className=''}}return}const hits=targets.map((t,i)=>{if(!entryTouched||t==null||!Number.isFinite(Number(t)))return false;if(Boolean(stored[i]))return true;return plan.state==='UP'?high>=Number(t):low<=Number(t)});if(lock&&plan.locked){lock.tpHits=hits;writeGoldTradeLock(lock)}const next=hits.findIndex((x,i)=>!x&&targets[i]!=null&&Number.isFinite(Number(targets[i])));for(let i=0;i<4;i++){const node=$('#goldEta'+(i+1));if(!node)continue;if(!plan.locked||targets[i]==null||!Number.isFinite(Number(targets[i]))){node.textContent='—';node.className='';continue}if(!entryTouched){node.textContent='بانتظار دخول النطاق';node.className='';continue}if(hits[i]){node.textContent='✓ تحقق';node.className='targetHit';continue}if(i===next){node.textContent=(etas[i]?'المدة: '+etas[i]+' • ':'')+'الهدف الحالي';node.className='targetCurrent'}else{node.textContent=i===3?'الامتداد الأخير للإشارة':'امتداد بعد الهدف '+i;node.className=''}}}";
  if(source.includes(currentFn))source=source.replace(currentFn,newFn);
  else if(source.includes(previousPatchedFn))source=source.replace(previousPatchedFn,newFn);
  else if(source.includes(oldFn))source=source.replace(oldFn,newFn);
  source=source.replaceAll('GOLD_LIVE_MASSIVE_CACHE_MS=4500','GOLD_LIVE_MASSIVE_CACHE_MS=900');
  source=source.replaceAll('setInterval(loadGold,5000)','setInterval(loadGold,1000)');
  source=source.replaceAll('setInterval(loadGold,20000)','setInterval(loadGold,1000)');
  source=source.replaceAll('setInterval(loadGold,30000)','setInterval(loadGold,1000)');
  return source;
}

fs.writeFileSync=function(path,data,...args){
  const p=String(path);
  if(!p.endsWith('/.runtime-server.mjs')&&!p.endsWith('\\.runtime-server.mjs'))return previousWriteFileSync(path,data,...args);
  const isBuffer=Buffer.isBuffer(data),patched=patchStickyGoldTargets(isBuffer?data.toString('utf8'):String(data));
  return previousWriteFileSync(path,isBuffer?Buffer.from(patched,'utf8'):patched,...args);
};

syncBuiltinESMExports();
await import('./gold-entry-range-start.js');
